apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
# kured DaemonSet - automatic node reboot manager
- kured.yaml
# NOTE: kube-vip.yaml is a static pod template copied to nodes by scripts, not applied via kubectl.
