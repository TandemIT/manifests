apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Apply Traefik first so the application ingresses and TCP routes have an ingress controller.
resources:
  - traefik/
  - cert-manager/
  - anubis/
  - gitea/
  - gitea-runner/
  - atlantis/
