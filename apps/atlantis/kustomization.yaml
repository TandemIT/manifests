apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Atlantis itself was removed (Gitea Actions/runners are the Terraform
# execution mechanism now). What's left is the namespace that Garage — the
# platform's shared S3-compatible storage — runs in.
resources:
  - namespace.yaml
  - garage
